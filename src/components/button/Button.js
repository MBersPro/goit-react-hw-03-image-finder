import React, {Component} from 'react'

class Button extends Component {
    state = {}
    render() {
        return (
            <button>
                Load More
            </button>
        );
    }
}

export default Button;